@model IPhoneStore.Domain.Entities.IPhone

<div class="well">
    <h3>
        <strong>@Model.Model</strong>
        <span class="pull-right label label-primary">@Model.Price.ToString("# грн")</span>
    </h3>
    <span class="lead">@Model.Description</span>
</div>